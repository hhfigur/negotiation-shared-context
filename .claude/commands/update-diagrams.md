Analysiere alle seit dem letzten Git-Commit geänderten Dateien (`git diff --name-only HEAD`).
Bestimme, welche der vier Abschnitte in `docs/ARCHITECTURE.md` davon betroffen sind 
(User Flows, Datenflüsse, Architektur/Service Boundaries, Error Handling).
Aktualisiere nur die betroffenen Abschnitte und ergänze das Änderungslog.
Gib am Ende eine Zusammenfassung aus, was sich geändert hat.

# Stage 2 — Migration Plan

This plan describes what a `MIGRATION_MODE: APPLY` run would do. **Nothing in this plan has been executed.** All actions are additive (new files) or merges (existing files gain sections; no existing content is deleted or overwritten) unless explicitly marked otherwise. Every phase lists: repository + path, action, exact source, dependencies, validation, rollback.

**Delta review (2026-09-04, pass 1 — Supabase/Lovable/Railway)**: the owner's three corrections remove one prior blocking dependency (Phase B/C's `.sdlc/config.yaml` `supabase.ownership_reference` field — no longer blocked on an owner decision, see the Phase B row below) and add one new pre-adoption correction (Phase A's controller docs row, below — Lovable Plan Mode must be stripped before adoption, it is not "no further adaptation required" as originally stated). Phase E gains a firmer evidentiary basis (owner-confirmed facts, not just cross-referenced repo evidence) for its Supabase and Railway/Lovable rows — see the updated rows below.

**Delta review (2026-09-04, pass 2 — C1 canonical API contract, C3 canonical closure)**: the migration kit itself was corrected in a separate authorized pass (Lovable/Railway/Supabase text fixed across 17 kit files, validator re-passed) — that work is out of scope for this report and not re-listed here. This pass resolves two further owner decisions. **C1**: `negotiationcoach-backend/docs/api-catalog.md` is now the confirmed canonical API contract (Phase C); the frontend's and shared-context's competing contract files both change role from "fix in place" to "reduce to pointer" (Phase B and Phase E). Also found on this pass's direct full read of `AUDIT_REFACTORING_CONTROLLER.md`: the "Lovable Plan Mode" reference the pass-1 delta review flagged in Phase A does **not** actually exist in that kit file — it belongs to shared-context's own (separate, older) `docs/governance/ALL-PROMPTS-AUDIT.md`. Phase D's row is corrected below; no kit edit was owed there after all. **C3**: `/negotiation-ai-sdlc:close-change` is now the confirmed canonical closure mechanism; the four existing close-task variants are LEGACY (Phase D, Phase E). A real, unresolved implementation gap was found in `close-change/SKILL.md` itself — see the new Phase D row and `10-conflicts-and-decisions.md` C3.

## Phase A — Shared-context SDLC scaffolding

| Repo + path | Action | Exact source | Dependencies | Validation | Rollback |
|---|---|---|---|---|---|
| `shared-context/sdlc/README.md`, `registry.yaml`, `changes/.gitkeep`, `policies/*` (7 files), `templates/*` (10 files), `scripts/*.py` (2 files) | ADD, **not verbatim — 3 further text corrections required (found on independent re-verification 2026-09-04, second pass)** | `shared-context-overlay/sdlc/*`, except: `registry.yaml`'s `repositories.{shared_context,frontend,backend}: <DISCOVER>` fields (filled per the decision point below); `policies/ux.md` lines 21 and 42 (drop the two Lovable references — no active Lovable design workflow); `policies/architecture.md` line 40 ("coupling between the two Supabase projects" → reworded, there is one shared project, not two); `templates/plan.md` line 44 (the `<LOVABLE_BOUNDARY_AND_UPDATE_METHOD>` placeholder → a generation-tool-neutral form). See `10-conflicts-and-decisions.md`'s addendum for exact wording. `registry.yaml` path fields: a decision point, since it's tracked and must not carry a machine-specific absolute path; recommend repo-relative or logical names only, with resolution left to each `.sdlc/config.local.yaml` | None — no existing files touched | `python3 sdlc/scripts/validate_kit.py` equivalent structural check already proven working in this venv; re-run kit validator against the installed copy; additionally re-read the 3 corrected policy/template files before install to confirm the edits landed | `git rm -r sdlc/{README.md,registry.yaml,changes,policies,templates,scripts}` on the migration branch, or simply don't merge the branch |
| `shared-context/controllers/DEVELOPMENT_CONTROLLER.md`, `AUDIT_REFACTORING_CONTROLLER.md`, `SESSION_HANDOFF_TEMPLATE.md` | ADD, with one required edit before adoption | `shared-context-overlay/controllers/*` (repository names `negotiation-Buddy`/`NegotiationCoach-backend` already used inside them match the real repo names). **Correction required (owner-confirmed 2026-09-04, supersedes the original "no further adaptation strictly required" note)**: `AUDIT_REFACTORING_CONTROLLER.md`'s invocation-path list names "Lovable Plan Mode" as a live venue — this must be removed before the file is adopted or pasted into a live Claude.ai Project, since Lovable is retired from active development. See `05-instruction-and-skill-map.md` delta review. | None | Manual read-through by the product owner before first live use, **including confirming the Lovable Plan Mode removal was made** (these become the pasted Claude.ai Project instructions) | Remove the 3 files |
| `shared-context/tooling/claude-marketplace/*` (marketplace.json, plugin source: 16 Skills, 8 agents, 7 reference docs) | ADD, **not verbatim — 3 required text corrections first** | `shared-context-overlay/tooling/claude-marketplace/*`, with `skills/assess-supabase/SKILL.md` line 17, `skills/create-plan/SKILL.md` line 31, and `skills/assess-ux/SKILL.md` line 15 corrected per `10-conflicts-and-decisions.md`'s "Kit Skill/template corrections required before adoption" table (owner-confirmed 2026-09-04: the kit's `assess-supabase` instruction directly contradicts the confirmed shared-Supabase-instance architecture, and two Skills still reference Lovable) | None (install-only per its own README, no code changes elsewhere, beyond the 3 text corrections) | `/plugin marketplace add <abs-path>/shared-context/tooling/claude-marketplace` then `/plugin install negotiation-ai-sdlc@negotiation-ai-local` then `/reload-plugins`, then `/plugin`, `/skills`, `/context`, `/hooks`, `/doctor` per the Stage 4 checklist; `claude plugin validate <PLUGIN_PATH> --strict` when CLI available; **additionally re-read the corrected `assess-supabase`/`create-plan`/`assess-ux` SKILL.md files before install to confirm the edits landed** | Uninstall via `/plugin uninstall`, remove marketplace registration, delete the directory |
| `shared-context/tooling/hook-design/HOOK_CANDIDATES.md` | ADD | `shared-context-overlay/tooling/hook-design/HOOK_CANDIDATES.md` verbatim | None | Documentation only — no runtime validation applicable | Remove the file |

## Phase B — Frontend repo overlay (`negotiation-buddy/`)

| Repo + path | Action | Exact source | Dependencies | Validation | Rollback |
|---|---|---|---|---|---|
| `.sdlc/config.yaml` | ADD | `repo-overlays/frontend/.sdlc/config.yaml.template`, with `supabase.ownership_reference` filled to point at the corrected `docs/infrastructure/supabase-instances.md` (Phase E, row 1) — **no longer blocked on an owner decision**: C2 is RESOLVED (2026-09-04), so this field can be finalized as soon as Phase E's Diff 1 lands, in the same sequence originally planned, just without the prior "pending decision" caveat | Phase A must land first (registry conventions); Phase E Diff 1 should land before or with this so the reference target is accurate on day one | Parse as YAML; confirm `logical_name`/`role` match `04-repository-registry.yaml` | `git rm .sdlc/config.yaml` |
| `.sdlc/config.local.yaml` | ADD, gitignored | `repo-overlays/frontend/.sdlc/config.local.yaml.template`, filled with the real local path `/Volumes/MF_extern/app-workspace/shared-context` | `.gitignore` update below must land first or simultaneously | Confirm `git status` does not show this file as trackable after the `.gitignore` change | Delete the file (never committed) |
| `.gitignore` | MERGE (append one line) | `repo-overlays/frontend/.gitignore.additions` → `.sdlc/config.local.yaml` | None | `git check-ignore .sdlc/config.local.yaml` returns success | `git diff` revert of the one added line |
| `REVIEW.md` | ADD | `repo-overlays/frontend/REVIEW.md.template` verbatim (no repo name currently exists to collide) | None | Manual read-through; used as the review checklist once the plugin's `review-change` Skill or the Audit Controller is exercised | `git rm REVIEW.md` |
| `.claude/rules/00-sdlc-contract.md` | ADD | `repo-overlays/frontend/.claude/rules/00-sdlc-contract.md` verbatim | None | N/A (prose rule file) | `git rm` |
| `.claude/rules/frontend-ui.md` | ADD, **path globs must be verified first, plus one Lovable-text correction** | `repo-overlays/frontend/.claude/rules/frontend-ui.md` — verify its frontmatter globs (`src/**/*.{ts,tsx,js,jsx,css,scss}`, `app/**/*...`, `components/**/*...`) against the real `negotiation-buddy/src/` layout before adoption; the template itself says "retain only path patterns that exist in this repository". **Also (owner-confirmed 2026-09-04, C4 RESOLVED)**: line 17's "Follow the verified design system, component conventions, tokens, and Lovable workflow" must drop "and Lovable workflow" — no active Lovable workflow exists (`10-conflicts-and-decisions.md`) | Requires a real directory listing of `src/` (already available from Stage 1 discovery — `src/pages`, `src/hooks`, `src/components` etc. were confirmed to exist; `app/` and top-level `components/` outside `src/` were **not** confirmed and should likely be dropped from the glob list) | N/A | `git rm` |
| `.claude/rules/frontend-tests.md` | ADD, same glob-verification caveat | `repo-overlays/frontend/.claude/rules/frontend-tests.md` (not read in full this pass — content unconfirmed, only its existence) | Same as above | N/A | `git rm` |
| `.claude/rules/api-contracts.md` | MERGE — **role changed by C1 RESOLVED (2026-09-04): reduce to pointer, not fix-in-place** | The base-URL/missing-endpoint factual fixes from the original plan (Railway→onrender.com; add `/api/enrich`/`/api/simulate/*`) are superseded by a smaller edit: replace the file's body with a short pointer to the canonical `../negotiationcoach-backend/docs/api-catalog.md` plus frontend-only implementation notes (client file `src/lib/apiClient.ts`, JWT-injection mechanism) — per owner rule "negotiation-buddy consumes the canonical contract but must not redefine it." See `10-conflicts-and-decisions.md` C1 and `09-proposed-file-diffs.md` | C1 canonical file (backend `docs/api-catalog.md`, Phase C below) should land first so the pointer target is accurate on day one | Confirm the file no longer restates full request/response shapes, only points to the canonical source | `git revert` |
| `CLAUDE.md` | MERGE (restructure) | `repo-overlays/frontend/CLAUDE.md.template` structure, populated with real verified commands from `03-command-and-toolchain-evidence.md` and links to existing `.claude/rules/*`/`docs/*` for detail. **Correction required (owner-confirmed 2026-09-04, C4 RESOLVED)**: template lines 42 ("Lovable-generated boundaries: `<DISCOVER_AND_LINK>`") and 51 ("Preserve Lovable and other generated-code boundaries; use the verified generation workflow") assume an active Lovable generation workflow — reword to state the historical-Lovable-origin/current-Claude-Code-development fact instead. **New (C1 RESOLVED 2026-09-04)**: the template's "Architecture orientation" list has no line pointing at the API contract — add one: "API contract (canonical, backend-owned): `../negotiationcoach-backend/docs/api-catalog.md`" (`10-conflicts-and-decisions.md`) | Requires the full current `CLAUDE.md` text (not yet read verbatim by this analysis — only summarized) before a literal merge diff can be written; see `09-proposed-file-diffs.md` | Read-through by the owner; `/context` check post-install to confirm no unintended duplication | `git revert` |

## Phase C — Backend repo overlay (`negotiationcoach-backend/`)

Same pattern as Phase B, mirrored:

| Repo + path | Action | Exact source | Dependencies | Validation | Rollback |
|---|---|---|---|---|---|
| `.sdlc/config.yaml`, `.sdlc/config.local.yaml`, `.gitignore` append | ADD | `repo-overlays/backend/.sdlc/*.template`, `.gitignore.additions` | Phase A | Same as frontend | Same as frontend |
| `REVIEW.md` | ADD | `repo-overlays/backend/REVIEW.md.template` (not read in full this pass — existence confirmed only) | None | Manual read-through | `git rm` |
| `.claude/rules/00-sdlc-contract.md` | ADD | `repo-overlays/backend/.claude/rules/00-sdlc-contract.md` verbatim (read in full — identical structure to frontend's, backend-worded) | None | N/A | `git rm` |
| `.claude/rules/backend-tests.md` | ADD, glob-verify | `repo-overlays/backend/.claude/rules/backend-tests.md` (not read in full — existence only) | Verify against real `tests/`/`src/` layout (Stage 1 confirmed `tests/layer1`, `tests/layer2`, `tests/layer3` exist) | N/A | `git rm` |
| `.claude/rules/supabase-data.md` | ADD, glob-verify | `repo-overlays/backend/.claude/rules/supabase-data.md` (read in full) — globs `supabase/**/*`, `migrations/**/*`, `database/**/*`, `db/**/*`, `**/*schema*`, `**/*generated*`; only `supabase/**/*` was confirmed to exist in this repo, others should likely be dropped | Same | N/A | `git rm` |
| `.claude/rules/backend-api-domain.md` | ADD | `repo-overlays/backend/.claude/rules/backend-api-domain.md` (not read this pass) | — | N/A | `git rm` |
| `.claude/rules/db-boundaries.md` | MERGE (fix stale path reference) | Not a template substitution — correct the `src/engine/simulationEngine.ts` reference to the real `src/layer1/*`/`src/layer3/*` paths, per Stage 1 finding | None | Grep confirms no remaining reference to the nonexistent path | `git revert` |
| `docs/api-catalog.md` | MERGE (add missing routes) — **confirmed canonical location, C1 RESOLVED (2026-09-04)** | This file is now the designated single canonical API contract for the whole project (owner decision, `10-conflicts-and-decisions.md` C1). Add the 6 missing `/api/simulate/*`/`/api/opponent-simulation/*` route entries — content for these already exists, fully written, in `shared-context/docs/contracts/frontend-backend.md` (read in full this pass) and can be adapted/relocated rather than re-authored from source; cross-check against `src/api/simulationRoutes.ts`/`src/api/opponentSimulationRoutes.ts` before finalizing. Also refresh the "Observed... 2026-03-27" header, since the file is now the authoritative single source, not a point-in-time snapshot | None — no other decision blocks this; C1 is resolved | Cross-check route list against actual route files; confirm no content was lost from `frontend-backend.md` in the merge | `git revert` |
| `CLAUDE.md` | MERGE (restructure) | `repo-overlays/backend/CLAUDE.md.template` structure + real verified commands. **New (C1 RESOLVED 2026-09-04)**: add an "API contract (canonical, this repository): `docs/api-catalog.md`" line to the template's "Architecture orientation" list, which currently has no explicit contract pointer | Full current-file read required first | Read-through, `/context` check | `git revert` |

## Phase D — Controller repositioning (cross-repo, shared-context-hosted)

| Item | Action | Dependencies | Validation | Rollback |
|---|---|---|---|---|
| Old Development Controller docs (`docs/governance/delivery-controller-{setup,grundinstruktion}.md`) | Proposed `SUPERSEDE_LATER` — annotate with a pointer to the new `controllers/DEVELOPMENT_CONTROLLER.md`, do not delete | Phase A must land | Owner confirms the new Controller doc is usable before the old one is de-referenced from any live Claude.ai Project | Remove the annotation |
| Old Audit Controller workflow docs (`docs/governance/GOV-01-audit-runbook.md`, `ALL-PROMPTS-AUDIT.md`, `README-AUDIT.md`) | Proposed `SUPERSEDE_LATER`, same pattern. **Correction to this row's own prior text (2026-09-04)**: an earlier revision of this plan claimed `AUDIT_REFACTORING_CONTROLLER.md`'s invocation list still names "Lovable Plan Mode" — re-verified false on direct read plus a kit-wide grep; that phrase exists only in this OLD shared-context doc (`ALL-PROMPTS-AUDIT.md`), not in the new kit template, so no kit-side edit was needed for it after all | Phase A | Same | Same |
| `controllers/DEVELOPMENT_CONTROLLER.md`'s "Source-of-truth contract" list | ADD one line — **C1 RESOLVED (2026-09-04)** | The list currently enumerates cross-repository lifecycle state, policies, frontend/backend implementation, and local paths, but has no line for the API contract. Add: "Canonical API contract: `NegotiationCoach-backend/docs/api-catalog.md` (backend-owned; Shared-context and negotiation-Buddy reference, do not redefine)." | Phase A; C1 resolution | Read-through | Remove the line |
| Legacy task-closing Skills (`shared-context/.claude/skills/{close-task,close-task-dev}/SKILL.md`, `negotiationcoach-backend/.claude/skills/close-task/SKILL.md`, `negotiation-buddy/.claude/skills/close-task/SKILL.md`) | ADD a `LEGACY` marker at the top of each file — **C3 RESOLVED (2026-09-04)** | Owner decision (`10-conflicts-and-decisions.md` C3): all four are LEGACY, not used for new SDLC-managed changes, not deleted, kept for traceability/rollback of already-closed items. Add a one-line frontmatter or header note to each: "LEGACY (superseded by `/negotiation-ai-sdlc:close-change`, 2026-09-04) — do not use for new SDLC-managed changes; preserved for traceability." No behavior change, no deletion, no rename | None | Confirm the marker doesn't break the Skill's existing frontmatter/invocation for old-style (non-SDLC) tasks that may still legitimately use it | Remove the marker line |
| `negotiation-ai-sdlc:close-change` Skill content | **Unresolved gap, not corrected this pass — kit-editing authorization was not extended to this session for this decision** | Direct read found `close-change/SKILL.md` implements outcome measurement (the `Released → Measure outcome` stage), not a pre-release closure gate re-check of intent/spec/plan/verification/review/no-open-CRITICAL-or-HIGH/traceability as the owner's rule 5 describes. See `10-conflicts-and-decisions.md` C3 for the two possible readings (distributed-gate-already-sufficient vs. needs-an-explicit-added-check) and `09-proposed-file-diffs.md` for the specific text that would be added under the second reading | Owner ruling on which reading applies | N/A — not applied | N/A — not applied |
| Live Claude.ai Projects (external, not repo content) | Not touched by this migration at all — reactivating them with the new instruction text is an owner action outside repo scope, explicitly deferred | — | — | — |

## Phase E — Targeted stale-content remediation (existing artifacts, not new files)

These are Stage 1 findings with clear, evidence-backed corrections, listed separately from Phases A–D because they are edits to *existing* content rather than overlay installation. All are `SUPERSEDE_LATER`/`MERGE` items from `02-existing-artifact-map.yaml`.

| Repo + path | Correction | Evidence | Risk if deferred |
|---|---|---|---|
| `shared-context/docs/infrastructure/supabase-instances.md` | Replace `ujnyioggxipvuxxxcivr` (labeled production) with `gpllrgkuozytyrmpfwbb` (**owner-confirmed 2026-09-04 as the single active project, intentionally shared by both repos** — no longer just cross-repo-evidence-inferred; also confirmed in both code repos' `MEMORY.md`/config) | `04-supabase-boundary-map.md`, owner statement (`10-conflicts-and-decisions.md` C2 RESOLVED) | **Highest** — a session trusting this file could target the wrong Supabase project for a real action |
| `negotiation-buddy/.claude/rules/api-contracts.md` | **Superseded by C1 (2026-09-04)**: reduce to pointer (see Phase B row above), not a base-URL/endpoint fix-in-place. The base-URL and missing-endpoint facts recorded here remain accurate evidence for *why* the pointer change is needed, but are no longer the action taken on this file | `01-current-inventory.md`, backend `docs/api-catalog.md` gap, owner statement | High — this is the frontend's self-declared canonical contract file and it's factually wrong |
| `negotiationcoach-backend/docs/api-catalog.md` | **Confirmed canonical, C1 RESOLVED (2026-09-04)**: add `/api/simulate/*` (3 routes), `/api/opponent-simulation/*` (3 routes) — see Phase C row above for the exact merge-from-`frontend-backend.md` approach | Same | High — violates the repo's own sync rule |
| `shared-context/docs/contracts/frontend-backend.md` | **New (C1 RESOLVED 2026-09-04)**: reduce from a full competing contract to a thin pointer to `NegotiationCoach-backend/docs/api-catalog.md`, retaining only genuinely cross-repo audit content (Type Drift Register, Known Contract Violations register) that doesn't restate the contract itself — mirrors the existing `product/audit/refactor-backlog.md` thin-pointer pattern | Backend `docs/api-catalog.md` completion (Phase C row above) should land first so nothing is lost in the reduction | Confirm the 6 simulate/opponent-simulation route specs were fully relocated to the backend file before this file's content is reduced | `git revert` |
| Railway→Render.com naming sweep, **and Lovable-as-active-dev-tool sweep** | `.claude/rules/architecture.md` and `data-access.md` (frontend), `docs/repo-map.md`, `docs/lovable-project-knowledge.md`, `docs/lovable-workspace-knowledge.md`, `docs/data-access-map.md`, `docs/audit-findings.md` F-007, `README.md`'s Lovable boilerplate (frontend); `docs/wiki/architecture.md`, `docs/wiki/WIKI---*.md` bundle, `docs/governance/*` (shared-context) | `01-current-inventory.md` cross-repo summary item 3, **owner statement confirming both Railway and Lovable-as-active-tool are retired (2026-09-04)** | **Confirmed** (was "Medium, systemic — misdirects"; now a certain documentation-debt fix, not a suspected one), systemic — misdirects any agent that reads these before code |
| `negotiationcoach-backend/docs/audit-findings.md` FINDING-A03 / AGENTS.md ARCH-01 | Mark resolved (CORS + auth-rejection both fixed in current code) | `01-current-inventory.md` backend findings | Low-medium — false-positive risk, wastes future audit effort |
| Legacy close-task Skills (4 files, all repos) | **New (C3 RESOLVED 2026-09-04)**: add `LEGACY` marker — see Phase D row above | Owner statement, `10-conflicts-and-decisions.md` C3 | Low — mislabeling risk only if a future session ignores the marker |

Each remediation requires reading the affected file's full current content before writing the corrected text. `docs/infrastructure/supabase-instances.md`, all three contract-related files (`negotiation-buddy/.claude/rules/api-contracts.md`, `negotiationcoach-backend/docs/api-catalog.md`, `shared-context/docs/contracts/frontend-backend.md`), and the four close-task Skills now have this level of evidence from direct full reads; the Railway-naming sweep still needs a simple grep-and-replace pass per file, not fabricated content.

## Phase F — Pilot change bootstrap (next action, not part of this ANALYZE_ONLY run)

Per the acceptance criteria's "Pilot" section and the master prompt's closing instruction: once Phases A–C are accepted and applied, bootstrap exactly one low-risk change through `sdlc/changes/<change-id>/` end-to-end (at least intent → spec → plan → implementation evidence → verification → review) using the `negotiation-ai-sdlc` plugin's Skills. A strong candidate, given this analysis's own findings, is the `docs/infrastructure/supabase-instances.md` correction itself (Phase E, row 1) — small, well-evidenced, high-value, and exercises the full lifecycle without touching production code.

## Sequencing

Phase A has no dependencies and can land first. Phases B and C depend only on Phase A's registry conventions and can proceed in parallel (separate repo windows, consistent with current working style). Phase D depends on Phase A. Phase E is independent of A–D and could be done immediately, but is sequenced after so the new lifecycle (accepted plan before implementation) can govern even this first batch of fixes — recommended as the actual Phase F pilot change rather than an ad hoc pre-migration fix.
